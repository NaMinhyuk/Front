<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LIFESHARING"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <activity android:name=".MainActivity" android:exported="true">
        </activity>
        <activity android:name=".product.Product_Menu_Activity" android:exported="true">
        </activity>

        <activity android:name=".product.Product_Detail_Activity" android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>

        <activity android:name=".product.Product_Detail_Reserve_Activity" android:exported="true">
        </activity>

        <activity android:name=".product_register.Regist_Add_Activity" android:exported="true">


        </activity>

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="9f2e646c60069e6855bcaa48ca28ff03" />

    </application>

<!--    인터넷 연결-->
    <uses-permission android:name="android.permission.INTERNET" />
<!--    GPS와 네트워크를 사용하여 사용자의 정확한 위치 정보를 가져온다.-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<!--    네크워크만 사용하여 사용자의 대략적인 위치 정보만을 가져온다.-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>


</manifest>