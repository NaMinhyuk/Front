<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <item
        android:id="@+id/homeFragment"
        android:icon="@drawable/ic_home_menu"
        android:enabled="true"
        android:title="홈"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction"/>

    <item
        android:id="@+id/reservationFragment"
        android:icon="@drawable/ic_reserve_menu"
        android:title="예약"
        app:showAsAction="always"/>

    <item
        android:id="@+id/registerFragment"
        android:icon="@drawable/ic_regist_menu"
        android:title="등록"
        app:showAsAction="always"/>

    <item
        android:id="@+id/messengerFragment"
        android:icon="@drawable/ic_messenger_menu"
        android:title="메신저"
        app:showAsAction="always"/>

    <item
        android:id="@+id/mypageFragment"
        android:icon="@drawable/ic_my_page_menu"
        android:title="마이 페이지"
        app:showAsAction="always"/>

</menu>